{
  "name": "furnlo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n docker,api,web,stripe -c magenta,blue,green,yellow \"docker-compose up chrome\" \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\" \"stripe listen --forward-to localhost:4000/api/webhooks/stripe\"",
    "dev:api": "docker-compose up chrome -d && npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run db:generate && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "build:api": "npm run db:generate && npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "start": "concurrently -n api,web -c blue,green \"npm run start --workspace=apps/api\" \"npm run start --workspace=apps/web\"",
    "prod": "npm run build && npm run start",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:generate": "npm run generate --workspace=packages/db",
    "db:migrate": "npm run migrate --workspace=packages/db",
    "db:migrate:deploy": "npm run migrate:deploy --workspace=packages/db",
    "db:studio": "npm run studio --workspace=packages/db"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "overrides": {
    "@types/express": "4.17.25",
    "@types/express-serve-static-core": "4.19.8"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
